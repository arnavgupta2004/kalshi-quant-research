# secrets - never commit credentials
.env
*.pem
*.key
# environments / caches
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.hypothesis/
# local data and run artifacts (large, regenerable)
var/
*.duckdb
*.duckdb.wal
*.sqlite
*.sqlite3
.DS_Store
# regenerable per-instance dump of the Stage 3 study (summary JSON + series stats are kept)
results/**/relation_instances.csv
